$(document).ready(function($) {

    $('.table').DataTable();
    // var rows = $('.table-row');
    // console.log(rows)

    // rows.DataTable()
    // $( '#name-th' ).click( function() {
    //     rows.sort(function(a, b) {
    //         return $(a).find('.name') < $(b).find('.name');
    //     })
    //     rows.each( function(index, row) {
    //         console.log(row)
    //     } )
    // } )

    // $( '#size-th' ).click( function() {
    //     rows.sort(function(a, b) {
    //         return $(a).find('.size').html() < $(b).find('.size').html();
    //     })
    //     rows.each( function(index, row) {
    //         console.log( $(row).find('.size').html() )
    //     } )
    // } )

});